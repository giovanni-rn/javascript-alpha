const guessNumber = (limit) => {
  // Générer un nombre aléatoire entre 0 et "limit"
  // ...

  let win = false;
  // Poser la question TANT QUE la réponse n'est pas trouvée
  // ...
  // // Vérifier les 3 cas : le nombre saisi est trop petit, trop grand ou correct
  // // S'il est correct la boucle doit s'arrêter
  // // ...
};

guessNumber(limit);
