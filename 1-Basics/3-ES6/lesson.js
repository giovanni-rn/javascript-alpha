// Créer une variable avec la syntaxe ES6
let nom = "valeur";
// console.log(nom);

// Les variables contantes
const voiture = "twingo";

// Créer une fonction avec la syntaxe ES6
const afficherNombre = (nb) => {
  console.log(nb);
};
// afficherNombre(15);

// return
const addNumber = (x, y) => {
  const sum = x + y;
  return sum;
};

const chiffre = addNumber(5, 37);
console.log(chiffre);
