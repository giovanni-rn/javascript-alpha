// Comment définir une variable
var nom = "valeur";
var fruit = "pastèque";
var chiffre = 4526;

// Afficher la valeur d'une variable dans la console
console.log(nom);
console.log(fruit, chiffre);

// Afficher du texte ou un nombre dans la console
console.log("texte");
console.log(2464);

// Utiliser les fonctions
function afficher() {
  var nouvelleVar = "Nouvelle valeur";
  console.log(nouvelleVar);
}
afficher();

function afficherNombre() {
  var nouveauNombre = 65;
  console.log(nouveauNombre);
}
afficherNombre();
afficherNombre();
afficherNombre();
afficherNombre();
