// Nombre aléatoire
const randomFloat = Math.random();
// console.log(randomFloat);

// Arrondissement d'un nombre décimal (float)
const floor = Math.floor(randomFloat * 10 + 1);
const ceil = Math.ceil(randomFloat * 10 + 1);
const round = Math.round(randomFloat * 10 + 1);
// console.log(floor, ceil, round);
console.log(Math.floor(Math.random() * 100 + 1));
